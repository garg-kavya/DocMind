"""
Pydantic API Schemas
=====================

Request and response schemas for the FastAPI endpoints.
These define the API contract — the shape of data entering and leaving the system.
Distinct from app.models which are internal domain objects.
"""
